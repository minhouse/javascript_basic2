//JavaScript basic 2
while (true) {
  //再代入される場合はletされない場合はconst今回はconst定義
  const result = prompt("日本の首都は？");
  if (result === "東京") {
    alert("正解です！");
  } else if (result === null) {
    alert("不正解です！");
  } else {
    alert("不正解です！");
  }
}
